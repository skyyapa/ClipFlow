# ClipFlow 版本说明

## 0.7.6（当前原型）

### 剪贴板记录

- 支持 Unicode 文本、HTML 和 RTF
- 支持 QQ、微信截图及常见 PNG/Bitmap 图片
- 图片转换为 PNG 并按 SHA-256 去重
- 使用 Bgr32 修复部分截图 Alpha 通道异常导致的灰色或透明缩略图
- 相同内容合并并更新复制时间、来源和次数
- 默认最多保留 5,000 条；收藏内容不自动清理
- 剪贴板被占用时有限重试，并避免记录自己写回的内容

### 搜索与粘贴

- `Ctrl + Shift + V` 全局呼出
- 关键词即时搜索，一次最多显示 100 条结果
- 搜索框为空时显示灰色“搜索剪贴板”提示词
- `↑` / `↓` 选择记录
- `Enter` 保留格式粘贴
- `Shift + Enter` 纯文本粘贴
- 支持图片再次粘贴
- 支持收藏、删除和清空未收藏记录

### 界面与窗口

- 使用接近 Windows 11 剪贴板的紧凑浅色浮窗和圆角卡片
- 图片缩略图按原始宽高比显示
- 使用 3px 浮动滚动指示器
- 标准滚轮一格约滚动 20px，触控板输入按比例移动
- 搜索框使用 8px 圆角、浅灰默认描边和蓝色聚焦描边
- 顶部空白区域支持拖动窗口
- 每次呼出自动定位到鼠标所在屏幕右下角任务栏上方
- 使用 Per-Monitor V2 DPI、像素对齐、布局取整和 ClearType
- 使用稳定的暖白实体背景与 DWM 圆角，不启用亚克力模糊

### 系统能力

- 托盘运行、暂停和恢复记录
- 单实例保护
- 多显示器和负坐标定位
- 本地 XML 历史与 PNG 图片持久化
- 支持通过 `CLIPFLOW_DATA_DIR` 覆盖数据目录

### 已验证

- 源码可成功构建为 `dist\ClipFlow.exe`
- 程序可启动并持续后台运行
- 外部复制文字可写入持久化历史
- 模拟截图可捕获、保存和再次粘贴
- 窗口可拖动，并可定位到当前屏幕任务栏上方

### 已知限制

- 暂不支持文件列表记录
- 暂无连续粘贴队列
- 暂无多行文本转 Excel/TSV 预览
- 快捷键、容量和保留天数暂不可在界面中配置
- 使用 XML 和内存线性搜索，尚未迁移到 SQLite FTS5
- 当前可执行文件没有代码签名

## 下一迭代

1. 设置界面与可配置保留策略
2. 文件列表记录与文件名搜索
3. 连续粘贴队列
4. 多行文本转 TSV 的表格预览
5. SQLite FTS5 存储与全文索引
