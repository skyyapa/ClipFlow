# ClipFlow 版本说明

## 0.9.0（当前原型）

### 文件列表

- 捕获 Windows `FileDrop` 文件和文件夹路径列表
- 文件路径进入 SQLite 与 FTS5，可按文件名、路径或“文件”搜索
- 相同路径集合自动去重并更新复制次数和来源
- 双击或按 `Enter` 可重新粘贴为真正的 Windows 文件对象
- 文件卡片显示首个文件的系统关联图标、名称和项目数量
- 更多菜单支持打开项目和在资源管理器中定位
- 文件被移动或删除时显示失效状态；粘贴时自动过滤无效路径

### 滚动条

- 滑块从卡片覆盖层移到独立的 10px 轨道
- 滑块宽度调整为 4px，不再与内容卡片或右侧按钮重叠
- 支持按住滑块用鼠标上下拖动
- 保留标准滚轮一格约 20px 的像素级滚动

### 验证

- 文件列表 SQLite 保存、去重、文件名搜索和类型搜索通过
- 真实 Windows 文件剪贴板监听通过
- FTS5 索引与主表记录数一致
- 滚动滑块拖动后列表偏移发生变化
- 完整应用构建与启动成功

## 0.8.0

- 历史数据迁移到 Windows 内置 SQLite
- 加入 WAL、常用字段索引和 FTS5 全文索引
- 兼顾 FTS5 前缀检索与中文子串匹配
- 旧 `history.xml` 自动导入并保留迁移备份
- 保留文本/图片去重、收藏保护、5,000 条容量与图片清理

## 0.7.6

- 支持 Unicode 文本、HTML、RTF 和常见剪贴板图片
- 修复 QQ/微信截图 Alpha 通道异常导致的灰色缩略图
- 加入全局呼出、搜索、自动粘贴、收藏、删除和托盘控制
- 完成 Windows 11 风格紧凑窗口、多屏定位和 Per-Monitor V2 DPI

## 下一迭代

1. 最小化图形设置界面
2. 自定义快捷键、容量与保留天数
3. 开机自启与敏感内容规则
4. 连续粘贴队列
5. 多行文本转 TSV 的表格预览
