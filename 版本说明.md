# ClipFlow 版本说明

## 0.14.0

- 多行文字卡片新增 `▦` 表格预览按钮，选中后也可按 `Ctrl+Enter` 打开
- 支持自动识别、每行一格、制表符、逗号、中文逗号、分号、竖线和连续空格
- 支持双引号字段、转义引号、未闭合引号保真、空字段、末尾空字段和混合 Windows/Unix 换行
- 预览展示行列数及前 30 行 × 8 列，最终粘贴保留完整数据
- 同时写入 TSV 纯文本与 CF_HTML 表格，提高 Excel、WPS 和 LibreOffice 兼容性
- 默认使用文本单元格格式保护前导零、长数字和公式样文字
- 表格结果可立即粘贴，也可加入 0.13.0 的连续粘贴队列

## 0.13.0

- 文字卡片新增 `+` 入队按钮，图片和文件可从“更多”菜单加入连续粘贴队列
- 队列栏显示剩余数量和下一条内容，可开始或清空队列
- 开始后第一条立即粘贴，后续每按一次全局呼出快捷键粘贴下一条
- 使用单一串行粘贴状态机，防止快速操作时剪贴板被后一个条目覆盖而乱序
- 写剪贴板失败、目标窗口失效或内容文件丢失时不移除当前条目，可继续重试
- 托盘菜单新增“取消连续粘贴队列”
- 粘贴成功后在后台原子增加使用次数，避免阻塞界面或覆盖计数
- 使用 Windows 剪贴板序列号替代单值回写标记，避免队列内容回录及误吞用户新复制内容
- 发送粘贴前再次核对剪贴板与目标窗口；内容被外部改写时保留队首，不误粘贴、不丢项
- 设置、表格预览、确认框和文件选择器显示期间屏蔽队列快捷键
- 修复剪贴板捕获重试后延迟没有恢复为 70ms 的问题

## 0.12.0

- 搜索栏右侧新增来源应用筛选，可直接使用现有历史记录中的来源信息
- 普通搜索和失效文件视图都支持按来源应用过滤
- 设置页新增“不记录这些应用”，支持逗号、分号或换行分隔进程名
- 应用排除对文本、图片和文件列表统一生效，并忽略 `.exe` 后缀与大小写差异
- 设置页新增可选的敏感单行文本保护，可跳过疑似验证码、银行卡号和高复杂度密码
- 敏感规则默认关闭，多行文本不会被该规则过滤
- 设置页改为固定底部操作按钮和可滚动表单，改善高 DPI 与小屏显示
- 新增来源筛选、应用排除和敏感规则的隔离回归测试

## 0.11.0

- 新增串行后台存储队列：图片编码、PNG 保存和剪贴板历史写入不再占用 UI 线程
- 图片编码移出数据库锁，搜索和界面刷新无需等待编码完成
- 主面板新增“失效文件”筛选，可集中查看路径已移动或删除的文件记录
- 支持批量清理未收藏的失效记录，收藏项会保留
- 文件列表可识别“部分路径失效”，仍可打开和粘贴其中有效的项目
- 文件条目“更多”菜单新增“重新定位失效路径…”
- 设置变更引发的容量裁剪和图片清理改为后台执行

## 0.10.3

- 关闭按钮改用 Windows 标准关闭字形
- 去除关闭按钮内边距，使符号在 30×30 按钮内水平、垂直居中

## 0.10.2

- 主面板右上角新增齿轮设置按钮，位于关闭按钮左侧
- 点击齿轮可直接打开单页设置窗口
- 保留系统托盘右键菜单中的“设置…”入口

## 0.10.1

- 修复文字卡片只有纯文本粘贴按钮、缺少鼠标收藏入口的问题
- 文字卡片右侧同时显示 `A` 纯文本粘贴按钮和图钉收藏按钮
- 保留选中条目后按 `Ctrl + S` 收藏或取消收藏

## 0.10.0

### 最小设置界面

- 托盘菜单新增“设置…”入口
- 支持开机自动启动
- 支持 `Ctrl+Shift`、`Ctrl+Alt`、`Alt+Shift` 与字母/数字组合快捷键
- 新快捷键被占用时自动恢复旧快捷键
- 最大历史条数可选 50、200、500、1,000、5,000、10,000 或 50,000
- 设置保存在本地 `settings.xml`

### 粘贴体验

- 文字卡片右侧新增 `A` 纯文本粘贴按钮
- 双击卡片继续直接粘贴并自动隐藏
- 保留 `Enter` 原格式粘贴与 `Shift + Enter` 纯文本粘贴

### 图片存储管理

- 可设置图片保留天数，0 表示不按日期清理
- 可设置未收藏图片空间上限，范围 10MB 至 10GB
- 超期图片和超过空间上限的最早图片自动删除记录与 PNG
- 收藏图片不参与自动清理
- 自动删除数据库中已无引用的孤立 PNG 文件
- 默认不按日期删除现有图片，避免升级后意外丢失

### 验证

- 完整应用构建成功
- 设置窗口可创建并正常显示
- SQLite/FTS5、文本、图片和文件列表回归测试通过
- 过期未收藏图片记录与 PNG 清理通过
- 收藏图片过期保护通过

## 0.9.0

- 支持 Windows 文件和文件夹列表捕获、搜索与再次粘贴
- 文件卡片显示关联图标，并可打开项目或所在位置
- 滚动条移至独立轨道并支持鼠标拖动

## 0.8.0

- 历史数据迁移至 Windows 内置 SQLite 与 FTS5
- 旧 XML 自动导入并保留迁移备份

## 0.7.6

- 支持文本、HTML、RTF、图片历史和 Windows 11 风格浮窗

## 下一迭代

1. 内容类型筛选
2. 历史导入、导出和备份
3. 收藏标题、标签和分组
4. 安装包、代码签名与自动更新
