# ClipFlow 0.7.6 原型

## 已完成

- 搜索框为空时显示灰色“搜索剪贴板”提示词，输入内容后自动隐藏
- Windows 后台剪贴板监听
- 文本、HTML 和 RTF 内容捕获
- QQ、微信截图及常见 PNG/Bitmap 剪贴板图片捕获
- 图片自动保存为 PNG、内容去重与缩略图展示
- 图片直接显示在历史卡片中，不使用独立预览区
- 界面改为接近 Windows 11 剪贴板的窄幅浅色浮层与圆角卡片
- 支持更多 PNG 剪贴板数据形式，并提高缩略图清晰度
- 修复部分电脑上图片卡片只有灰色占位的问题：改为直接读取 PNG 字节并在内存中解码
- 图片卡片改用独立路径转换器直接渲染，不再依赖历史对象的缩略图属性与缓存
- 修复 QQ/微信截图 RGB 有效但 Alpha 通道全为 0，导致 WPF 将整张图渲染为透明灰块的问题
- 图片列表改为直接创建控件，不再经过动态图片绑定
- 浮层压缩为约 360 个界面单位宽，匹配 Windows 11 原生面板比例
- 图片卡片总高压缩到约 80 个界面单位，左侧缩略图、右侧操作区
- 顶部改为“剪贴板 / 全部清除”，选中卡片使用黑色描边
- 固定图标改为倾斜图钉轮廓，未固定为空心、已固定为实心
- 支持按住顶部标题区域拖动窗口
- 按 Windows 原生面板补齐“表情符号等”标题、视图按钮、六项标签栏和关闭按钮
- 搜索框作为 ClipFlow 扩展功能插入剪贴板标题下方
- 缩略图按原始宽高比自动占宽，不再使用固定灰色框
- 使用细滚动条、原生选中描边和轻微卡片阴影
- 删除“表情符号等”右侧两个多余方形按钮
- 六个顶部标签图标改为独立矢量绘制，不再使用近似字符
- 滚动条使用完整自定义模板，可见轨道宽度固定为 4
- 删除整行顶部符号标签，只保留 ClipFlow、关闭、剪贴板和全部清除
- 拖动改为应用内鼠标位移跟踪，并让整条标题空白区域参与命中
- 已通过自动拖动测试，窗口坐标可随鼠标移动
- 每次呼出时自动定位到鼠标所在屏幕的右下角、任务栏工作区上方
- 使用 Windows 物理坐标定位，兼容负坐标、上下排列及不同缩放的多显示器
- 已验证实际右侧间距 8px、底部任务栏间距 9px
- 背景改为 Windows 原生亚克力模糊与浅色半透明材质
- 圆角改由 DWM 统一绘制，移除内层重复圆角底板和多余背景块
- 隐藏系统滚动条，改用独立的 3px 浮动滚动指示器
- 亚克力从旧式 Accent API 改为 Windows 11 原生 transient system backdrop
- 使用浅色白雾蒙版并降低内容层不透明度，避免发灰和重度磨砂
- 旧版 Windows 自动回退到更浅的兼容亚克力
- 撤销在 WPF 无边框窗口中表现不稳定的亚克力与玻璃扩展
- 恢复稳定的 Windows 11 暖白实体背景，保留单层系统圆角与 3px 滚动指示器
- 历史列表改为像素级滚动，不再按整张卡片跳转
- 滚轮标准一格移动约 20px，触控板细小输入按比例移动
- 加入 Per-Monitor V2 高 DPI 清单，避免 Windows 在 125%/150%/200% 缩放时整体拉伸窗口
- 启用像素对齐、布局取整和 ClearType 显示型文字渲染
- 跨显示器时按各屏 DPI 原生重绘
- 搜索框改为 8px 圆角，未聚焦浅灰描边、聚焦蓝色描边
- 历史图片再次粘贴
- 相同内容自动合并并更新时间
- 本地持久化，默认最多保留 5,000 条
- `Ctrl+Shift+V` 全局呼出
- 关键词即时搜索
- 键盘选择与自动粘贴
- 纯文本粘贴
- 收藏、删除和清空未收藏历史
- 托盘运行与暂停记录
- 单实例保护
- 多屏和高 DPI 窗口定位
- 剪贴板占用重试与自身写入防重复

## 已验证

- 源码编译成功
- 应用启动后可持续后台运行
- 外部复制文本能够被监听并写入持久化历史
- 模拟截图能够被捕获、保存成 PNG 并写入历史

## 下一迭代

1. 文件列表记录与文件名搜索
2. 连续粘贴队列
3. 多行文本转 TSV 的表格预览
4. 快捷键、容量和保留天数设置
5. SQLite FTS5 存储与全文索引
